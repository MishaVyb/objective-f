import { Flex } from '@radix-ui/themes'
import ProjectsList from '../components/projects'

const HomePage = () => {
  return (
    <Flex>
      <ProjectsList />
    </Flex>
  )
}

export default HomePage
